<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="HealthAssist.Views.OnboardingPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HealthAssist.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <ScrollViewer>
        <StackPanel Margin="40" MaxWidth="600" HorizontalAlignment="Center">
            <TextBlock Text="Welcome to Medication Reminder" 
                       FontSize="32" 
                       FontWeight="Bold" 
                       TextAlignment="Center" 
                       Margin="0,0,0,20"/>

            <TextBlock Text="Let's set up your profile" 
                       FontSize="18" 
                       TextAlignment="Center" 
                       Margin="0,0,0,40" 
                       Foreground="Gray"/>

            <TextBox x:Name="NameTextBox"
                     Header="Full Name"
                     PlaceholderText="Enter your full name"
                     Margin="0,0,0,20"/>

            <DatePicker x:Name="DateOfBirthPicker"
                        Header="Date of Birth"
                        Margin="0,0,0,20"/>

            <ComboBox x:Name="BloodTypeComboBox"
                      Header="Blood Type"
                      Margin="0,0,0,20"
                      MinWidth="200">
                <ComboBoxItem Content="A+"/>
                <ComboBoxItem Content="A-"/>
                <ComboBoxItem Content="B+"/>
                <ComboBoxItem Content="B-"/>
                <ComboBoxItem Content="AB+"/>
                <ComboBoxItem Content="AB-"/>
                <ComboBoxItem Content="O+"/>
                <ComboBoxItem Content="O-"/>
                <ComboBoxItem Content="Unknown"/>
            </ComboBox>

            <TextBox x:Name="AllergiesTextBox"
                     Header="Allergies (if any)"
                     PlaceholderText="List any known allergies"
                     TextWrapping="Wrap"
                     AcceptsReturn="True"
                     MinHeight="80"
                     Margin="0,0,0,20"/>

            <ComboBox x:Name="GenderComboBox"
                      Header="Gender"
                      Margin="0,0,0,20"
                      MinWidth="200">
                <ComboBoxItem Content="Male"/>
                <ComboBoxItem Content="Female"/>
                <ComboBoxItem Content="Other"/>
                <ComboBoxItem Content="Prefer not to say"/>
            </ComboBox>

            <Grid Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <NumberBox x:Name="HeightNumberBox"
                           Header="Height (cm)"
                           PlaceholderText="170"
                           SpinButtonPlacementMode="Compact"
                           Minimum="50"
                           Maximum="300"
                           Grid.Column="0"/>

                <NumberBox x:Name="WeightNumberBox"
                           Header="Weight (kg)"
                           PlaceholderText="70"
                           SpinButtonPlacementMode="Compact"
                           Minimum="10"
                           Maximum="500"
                           Grid.Column="2"/>
            </Grid>

            <Button x:Name="CompleteOnboardingButton"
                    Content="Complete Setup"
                    Style="{StaticResource AccentButtonStyle}"
                    HorizontalAlignment="Center"
                    Margin="0,20,0,0"
                    Click="CompleteOnboardingButton_Click"/>

            <ProgressRing x:Name="LoadingRing"
                          IsActive="False"
                          Visibility="Collapsed"
                          HorizontalAlignment="Center"
                          Margin="0,20,0,0"/>
        </StackPanel>
    </ScrollViewer>
</Page>
